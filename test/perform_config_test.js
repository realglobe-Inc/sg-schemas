/**
 * Test case for performConfig.
 * Runs with mocha.
 */
'use strict'

const performConfig = require('../lib/perform_config.json')
const assert = require('assert')
const co = require('co')

describe('perform-config', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Perform config', () => co(function * () {

  }))
})

/* global describe, before, after, it */
